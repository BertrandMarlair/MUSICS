import {unit, linear, radius, white, boxShadow} from "../../style/Constant";

export const CardStyle = {
    card: {
        margin: unit * 5,
        padding: unit * 5,
        background: linear,
        minHeight: 150,
        borderRadius: radius,
        color: white,
        boxShadow,
    },
};
